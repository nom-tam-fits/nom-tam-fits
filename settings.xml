<settings>
  <servers>
    <server>
      <id>nexus</id>
      <username>${env.PORTAL_USERNAME}</username>
      <password>${env.PORTAL_PASSWORD}</password>
    </server>
    <server>
      <id>central</id>
      <username>${env.PORTAL_USERNAME}</username>
      <password>${env.PORTAL_PASSWORD}</password>
    </server>
  </servers>
</settings>
