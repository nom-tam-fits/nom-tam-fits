<settings>
  <servers>
    <server>
      <id>snapshot-repo</id>
      <username>${env.PORTAL_USERNAME}</username>
      <password>${env.PORTAL_PASSWORD}</password>
    </server>
    <server>
      <id>central-repo</id>
      <username>${env.PORTAL_USERNAME}</username>
      <password>${env.PORTAL_PASSWORD}</password>
    </server>
  </servers>
</settings>
